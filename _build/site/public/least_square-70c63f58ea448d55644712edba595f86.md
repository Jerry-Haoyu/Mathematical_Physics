# Least Square 

Suppose we have a dataset $[(x_1,y_1),...,(x_n, y_n)]$, and by some prior knowledge we know there exists some relationship $y=kx + b$ in the dataset, the question is, how to find this particular relationship ? 

We know that we can reformulate 
